# cstderrno.h

@Metadata {
    @TitleHeading("Header File")
}

Imports `errno.h` with the `errno_t` type, regardless of compiler and library
support for ISO/IEC 9899:2011 (C11) Annex K.

## Topics

- ``errno_t``
